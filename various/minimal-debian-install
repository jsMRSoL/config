1. Download netinst (about 250-300MB)

2. Set up networking
vi /etc/network/interfaces
# add custom lines for ethernet
auto eth0
iface eth0 inet dhcp

# make interfaces file more secure
chmod 0600 /etc/network/interfaces

# work out preshared key
wpa_passphrase myssid my_very_secret_passphrase

# add lines for wifi
auto wlan0
iface wlan0 inet dhcp
        wpa-ssid myssid
        wpa-psk ccb290fd4fe6b22935cbae31449e050edd02ad44627b16ce0151668f5f53c01b

# restart networking
/etc/init.d/networking restart

3. Install sudo
apt-get install sudo
# open visudo for editing
su
visudo
# add custom lines
simon   ALL=(ALL:ALL) ALL
simon ALL=(ALL) NOPASSWD:/sbin/poweroff,/sbin/reboot,/sbin/shutdown


4. Install xorg

5. Install minimal gnome desktop environment
apt-get install –without-recommends gdm3

6. Install various packages
apt-get install build-essential alsa-utils ssh git

6. Install dwm
7. Customize bashrc
8. Install vim, ranger, mpd, ncmpdpp
